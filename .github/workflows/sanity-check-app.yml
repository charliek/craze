name: sanity-check-app

# Manual verification that the release-bot App is installed on every repo
# craze's release.yml touches:
#   1. charliek/craze itself (the running repo's secrets must resolve)
#   2. charliek/homebrew-tap (GoReleaser's brews step pushes Formula/craze.rb)
#   3. charliek/apt-charliek (publish dispatch sent at end of release)
#
# Run from the Actions UI ("Run workflow") after setting the
# RELEASE_BOT_APP_* secrets and confirming the App is installed on all
# three repos.
#
# NOTE: workflow_dispatch-only workflows can only be run once the file
# exists on the default branch — this can't be dispatched from this
# feature branch. Run it after this PR merges and before tagging v0.0.1.
#
# Useful when:
#   * first wiring the App into a new repo (verify before touching release.yml)
#   * rotating the App's private key (re-verify after updating the secret)
#   * migrating to a new App
#   * after adopting a new cross-repo target
#
# Each block mints a short-lived installation token scoped to one repo and
# calls a read-only endpoint. A failure to mint, or a 404 on the reach
# check, means the App isn't installed on that repo — fix the install
# before the next release runs.
#
# Bot identity comes from the action's `app-slug` output, NOT
# `gh api /user`, which returns 403 for installation tokens (they don't
# authenticate as a user).

on:
  workflow_dispatch: {}

permissions: {}   # this workflow needs nothing from the workflow token

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # ── 1. self (charliek/craze) ──
      - id: self-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.RELEASE_BOT_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_BOT_APP_KEY }}
          # Without a permission-* input the minted token carries every
          # permission the installation has. This check only reads repo and
          # installation metadata, so that is all it should be able to do.
          permission-metadata: read

      - name: Token sees craze + bot identity
        env:
          GH_TOKEN: ${{ steps.self-token.outputs.token }}
          BOT_SLUG: ${{ steps.self-token.outputs.app-slug }}
        run: |
          set -euo pipefail
          echo "::group::This repo (must print charliek/craze)"
          gh api "/repos/${GITHUB_REPOSITORY}" --jq '.full_name'
          echo "::endgroup::"

          echo "::group::Installation repository count"
          # Per-repo selective installs (the typical pattern) show count=1
          # from each repo's own installation perspective — the App's
          # installation on charliek/craze sees just craze. Cross-repo
          # minting (the per-target blocks below) reaches OTHER
          # installations via the App's private key + owner+repositories
          # inputs. So this count is informational only; the per-target
          # reach checks below are the actual verification of whether the
          # App can reach homebrew-tap and apt-charliek.
          gh api /installation/repositories --jq '.total_count'
          echo "::endgroup::"

          # Installation tokens can't call /user (403 "Resource not accessible
          # by integration") — bot identity comes from the action's app-slug.
          echo "::group::Bot identity (should end with [bot])"
          echo "${BOT_SLUG}[bot]"
          echo "::endgroup::"

      # ── 2. charliek/homebrew-tap (GoReleaser brews push target) ──
      - id: tap-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.RELEASE_BOT_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_BOT_APP_KEY }}
          owner: charliek
          repositories: homebrew-tap
          # The reach check (gh api GET /repos/...) only needs metadata
          # read; real release uses contents:write to push the formula.
          # We mint the broader scope here so the verification matches
          # what release.yml actually mints, catching scope-related
          # misconfigurations early.
          permission-contents: write

      - name: Token can reach charliek/homebrew-tap
        env:
          GH_TOKEN: ${{ steps.tap-token.outputs.token }}
        run: |
          set -euo pipefail
          echo "::group::charliek/homebrew-tap (must print the same)"
          gh api "/repos/charliek/homebrew-tap" --jq '.full_name'
          echo "::endgroup::"

      # ── 3. charliek/apt-charliek (publish dispatch target) ──
      - id: apt-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.RELEASE_BOT_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_BOT_APP_KEY }}
          owner: charliek
          repositories: apt-charliek
          # Matches what release.yml mints; surfaces scope mismatches early.
          permission-contents: write

      - name: Token can reach charliek/apt-charliek
        env:
          GH_TOKEN: ${{ steps.apt-token.outputs.token }}
        run: |
          set -euo pipefail
          echo "::group::charliek/apt-charliek (must print the same)"
          gh api "/repos/charliek/apt-charliek" --jq '.full_name'
          echo "::endgroup::"
